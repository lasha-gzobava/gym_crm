server.port: 8082
spring:
  application.name: trainer-workload-ms
  datasource:
    url: jdbc:h2:mem:workload;DB_CLOSE_DELAY=-1
  jpa:
    hibernate.ddl-auto: update
    properties.hibernate.format_sql: true

#eureka:
#  client:
#    service-url.defaultZone: http://localhost:8761/eureka

#security.jwt.secret: "BASE64_32+_BYTE_SECRET_HERE" # same secret used by monolith

logging.pattern.level: "%5p tx=%X{txId}"
